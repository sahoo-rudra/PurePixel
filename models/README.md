# Model Weights Location

Model weights are located at: ../weights/best_model.pth

This project uses "weights/" as the primary weights directory
(see repository structure in main README.md). The organizer-suggested
"models/" folder name is documented here for clarity -- run.py loads
from "weights/best_model.pth" directly.
